---
/**
 * Mixed breed discovery grid.
 * Current repo-safe defaults:
 * - hub: /breeds
 * - details: /breeds/[slug]
 */

export interface MixedBreedCard {
  slug: string;
  name: string;
  imageUrl?: string;
  img?: string;
  parent1?: string;
  parent2?: string;
  size?: string;
  energy?: string;
  trainability?: string;
  isPoodleMix?: boolean;
  href?: string;
}

export interface Props {
  id?: string;
  eyebrow?: string;
  title?: string;
  subtitle?: string;
  viewAllHref?: string;
  viewAllLabel?: string;
  detailRoutePrefix?: string;
  items?: MixedBreedCard[];
}

const {
  id = 'home-mixed-breed-discovery',
  eyebrow = 'Designer & mixed breeds',
  title = 'Popular mixed breed guides',
  subtitle = 'Explore mixed-breed profiles, likely trait blends, and practical care guidance based on parent-breed patterns.',
  viewAllHref = '/breeds',
  viewAllLabel = 'Browse mixed breeds',
  detailRoutePrefix = '/breeds',
  items = [],
} = Astro.props;
---

<section id={id} class="bg-[var(--color-primary-subtle)] py-12 sm:py-14 lg:py-16">
  <div class="mx-auto max-w-7xl px-4 sm:px-5 lg:px-6">
    <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
      <div class="max-w-3xl">
        <p class="text-xs font-bold uppercase tracking-[0.16em] text-[var(--color-primary)]">
          {eyebrow}
        </p>
        <h2 class="mt-3 text-3xl font-bold tracking-tight text-[var(--color-heading)] sm:text-4xl">
          {title}
        </h2>
        <p class="mt-3 text-base leading-7 text-[var(--color-body-soft)]">
          {subtitle}
        </p>
      </div>

      <a
        href={viewAllHref}
        class="inline-flex items-center gap-2 text-sm font-semibold text-[var(--color-primary)] transition hover:text-[var(--color-primary-hover)]"
      >
        {viewAllLabel}
        <span aria-hidden="true">→</span>
      </a>
    </div>

    <div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      {items.map((item) => {
        const href = item.href ?? `${detailRoutePrefix}/${item.slug}`;
        const image = item.imageUrl ?? item.img ?? '';
        return (
          <a
            href={href}
            class="group flex h-full flex-col overflow-hidden rounded-2xl border border-[var(--color-primary-light)] bg-[var(--color-bg)] shadow-sm transition hover:-translate-y-[2px] hover:border-[var(--color-border-primary)] hover:shadow-md"
          >
            <div class="relative h-40 overflow-hidden bg-[var(--color-primary-light)]">
              {image ? (
                <img
                  src={image}
                  alt={item.name}
                  class="h-full w-full object-cover transition duration-300 group-hover:scale-[1.03]"
                  loading="lazy"
                  decoding="async"
                />
              ) : (
                <div class="flex h-full w-full items-center justify-center text-5xl">🐾</div>
              )}

              {item.isPoodleMix && (
                <span class="absolute left-3 top-3 inline-flex rounded-full bg-white/90 px-2.5 py-1 text-[11px] font-bold uppercase tracking-[0.14em] text-[var(--color-primary)]">
                  Poodle mix
                </span>
              )}
            </div>

            <div class="flex flex-1 flex-col p-5">
              <h3 class="text-lg font-bold text-[var(--color-heading)]">
                {item.name}
              </h3>

              {(item.parent1 || item.parent2) && (
                <p class="mt-2 text-sm font-semibold text-[var(--color-primary)]">
                  {[item.parent1, item.parent2].filter(Boolean).join(' × ')}
                </p>
              )}

              <div class="mt-4 flex flex-wrap gap-2">
                {item.size && (
                  <span class="inline-flex rounded-full bg-[var(--color-accent-light)] px-2.5 py-1 text-xs font-semibold text-[var(--color-accent-active)]">
                    {item.size}
                  </span>
                )}
                {item.energy && (
                  <span class="inline-flex rounded-full bg-[var(--color-success-light)] px-2.5 py-1 text-xs font-semibold text-[var(--color-success)]">
                    {item.energy} energy
                  </span>
                )}
                {item.trainability && (
                  <span class="inline-flex rounded-full bg-[var(--color-bg-muted)] px-2.5 py-1 text-xs font-semibold text-[var(--color-body)]">
                    {item.trainability} training
                  </span>
                )}
              </div>

              <span class="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-[var(--color-primary)] transition group-hover:translate-x-[2px]">
                View mixed breed guide
                <span aria-hidden="true">→</span>
              </span>
            </div>
          </a>
        );
      })}
    </div>
  </div>
</section>
